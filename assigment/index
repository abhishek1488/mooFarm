// Main router entry point, sets up all route modules

const express = require('express');
const router = express.Router();

const indexRouter = require('./employeeRouter');


router.use('/employee', indexRouter);

module.exports = router;
