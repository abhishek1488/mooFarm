// All /albums routes

const express = require('express');
const router = express.Router();

const { addEmployee,getDetail } = require('../controllers/employeeController');


router.post('/', addEmployee);
router.get('/list', getDetail);

module.exports = router;